import { Typography } from "@mui/material";
import React, { useState, useEffect } from "react";

export default function NoMatch() {
  return (
    <div>
      <Typography variant="h1">NoMatch</Typography>
    </div>
  );
}
